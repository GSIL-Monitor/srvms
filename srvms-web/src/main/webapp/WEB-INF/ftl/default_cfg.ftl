<#assign ctxPath=request.contextPath>
<#assign attachUrl="${oss.url}">
<#assign resourceaddress="${zl.web.resource.address}">
<#setting number_format="#.##">
<script>
    var ctxPath = '${ctxPath}';
    var attachUrl = '${oss.url}';
    var resourceaddress = '${resourceaddress}';
</script>





<#assign ctxPath=request.contextPath>
<#assign attachUrl="${oss.url}">
<#assign resourceaddress="${zl.web.resource.address}">
<#setting number_format="#.##">
<script>
    var ctxPath = '${ctxPath}';
    var attachUrl = '${oss.url}';
    var resourceaddress = '${resourceaddress}';
</script>